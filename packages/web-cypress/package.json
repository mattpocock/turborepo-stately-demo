{
  "name": "@stately/web-cypress",
  "version": "0.1.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "lint": "tsc",
    "test": "echo \"No test needed!\"",
    "test:cypress": "start-server-and-test \"(cd ../../apps/web && next start)\" http://localhost:3000 \"cypress run\""
  },
  "dependencies": {
    "@stately/web": "*"
  },
  "devDependencies": {
    "cypress": "^9.6.0",
    "start-server-and-test": "^1.14.0"
  }
}
